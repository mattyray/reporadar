# Django
Django>=5.1,<6.0
djangorestframework>=3.15,<4.0
django-cors-headers>=4.4,<5.0

# Auth (allauth handles OAuth + headless JWT — no dj-rest-auth or simplejwt needed)
django-allauth[socialaccount]>=65.0,<66.0
PyJWT>=2.8,<3.0
cryptography>=43.0,<44.0

# Database
psycopg[binary]>=3.2,<4.0
dj-database-url>=2.2,<3.0

# Encryption (API key storage)
django-fernet-encrypted-fields>=0.3,<1.0

# Cache + Task Queue
redis>=5.0,<6.0
django-redis>=5.4,<6.0
celery[redis]>=5.4,<6.0

# HTTP client (for GitHub, Hunter, Apollo API calls)
requests>=2.32,<3.0

# AI
anthropic>=0.40,<1.0

# Server
gunicorn>=22.0,<23.0

# Testing
pytest>=8.0,<9.0
pytest-django>=4.8,<5.0
factory-boy>=3.3,<4.0
responses>=0.25,<1.0
freezegun>=1.3,<2.0
pytest-cov>=5.0,<6.0

# Dev utilities
python-dotenv>=1.0,<2.0
