# Django
SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_SETTINGS_MODULE=config.settings.development

# Database
DATABASE_URL=postgresql://reporadar:reporadar@db:5432/reporadar

# Redis
REDIS_URL=redis://redis:6379/0

# Encryption key for API key storage (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
FIELD_ENCRYPTION_KEY=

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Claude API (resume parsing + outreach generation)
ANTHROPIC_API_KEY=

# CORS (frontend URL)
CORS_ALLOWED_ORIGINS=http://localhost:5173
